# pact: the event log is append-only, so a merge keeps BOTH sides
# rather than stopping for a human who has nothing to decide.
.pact/events.jsonl merge=union

# pact: these logs are append-only, so a merge keeps BOTH sides
# rather than stopping for a human who has nothing to decide.
.pact/messages.jsonl merge=union
